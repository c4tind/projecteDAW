@model IEnumerable<ArmyHammerProject.Models.MinisPerLlista>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Llista.Nom)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Miniatura.Nom)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Quantitat)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Llista.Nom)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Miniatura.Nom)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Quantitat)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ID })
        </td>
    </tr>
}

</table>
